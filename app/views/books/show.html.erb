<div>
  <p><%= image_tag 'no_image.jpg' %></p>
  <p><%= @books.user.name %></p>
  <p><%=link_to @books.title, book_path(@books.id)%></p>
  <p><%= @books.body %></p>
  <% if @books.user == current_user %>
    <%= link_to "Destroy", book_path(@books), method: :delete %>
  <% end %>
  
  
</div>
